# Email_spam_delection.
This project classifies emails as spam or ham using machine learning. Email text is cleaned and converted into numerical features using TF-IDF. A Multinomial Naive Bayes model is trained and evaluated for accuracy and can predict spam or ham for new email messages.
